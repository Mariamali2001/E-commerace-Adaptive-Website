theme: {
  extend: {
    colors: {
      brand: { DEFAULT: "#0B0B0B" },     // buttons, headers
      muted: "#6B7280",
      card: "#FFFFFF"
    },
    borderRadius: { xl: "0.75rem", "2xl": "1rem" },
    boxShadow: {
      card: "0 10px 30px rgba(0,0,0,.06)"
    }
  }
}