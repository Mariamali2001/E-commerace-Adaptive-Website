# Copy to .env.local and fill in keys.
# Live shop does NOT need these — only offline variant generation.

# gemini (default for testing) | openai (switch later)
LLM_PROVIDER=gemini

# Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY= AIzaSyDNujQKihi8qcusIT-LUmF9fOIdxzDMTE0
GEMINI_MODEL=gemini-2.0-flash

# When ready to switch back:
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
